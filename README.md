# 🤝 Volunteering Management Platform

A robust Full-Stack application developed to facilitate the connection between volunteers and event organizers. The project manages the complete lifecycle of a volunteering activity, from event creation to enrollment and the review system.

---

## 🚀 Key Features

* **Event Management:** Organizers can create, update, and delete events (complete CRUD).
* **Review System:** Volunteers can leave feedback and ratings for events they attended.
* **Volunteer Profiles:** Each user has a dedicated profile to track their activity history.
* **Database Architecture:** Relational structure optimized for data consistency.
* **Responsive Interface:** Dynamic frontend built with JavaScript for a fluid user experience.

---

## 🛠️ Technologies Used

This project is built using a modern Java-based stack:

* **Backend:** `Java`, `Spring Boot` (Web, Data JPA)
* **Database:** `PostgreSQL`, `Hibernate` (ORM)
* **Frontend:** `JavaScript`, `HTML5`, `CSS3`
* **Build Tool:** `Maven`
* **Version Control:** `Git`

---

## ⚙️ Setup and Installation

To run the project locally, follow the steps below:

### 1. Clone the repository
```bash
git clone [https://github.com/tudor-stack/your-repo-name.git](https://github.com/tudor-stack/your-repo-name.git)
cd your-repo-name

### 2. Instal dependencies
```bash
nmp install


### 3. Enviroment configuration
```bash
Create a .env file in your project and add the followings:

DATABASE_URL=your_db_url
API_KEY=your_secret_key
